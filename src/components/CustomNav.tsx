import styled from "styled-components";

export const CustomNav = styled.ul({
  justifyContent: "space-between",
  display: "flex",
  listStyle: "none",
  fontSize: 20,
  padding: 30,
  backgroundColor: "lightblue",
});
